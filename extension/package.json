{
  "name": "@shopgate/search-cloudsearch",
  "version": "1.1.3",
  "description": "",
  "license": "Apache-2.0",
  "scripts": {
    "test": "mocha --recursive test/unit",
    "cover": "nyc --reporter=html mocha --recursive test/unit",
    "integration": "mocha --timeout 5000 --use_strict --recursive test/integration",
    "standard": "standard"
  },
  "devDependencies": {
    "mocha": "^6.1.4",
    "nock": "^10.0.6",
    "nyc": "^14.1.1",
    "proxyquire": "^2.1.1",
    "sinon": "^7.3.2",
    "standard": "^13.0.2"
  },
  "dependencies": {
    "hyphenation.de": "^0.2.1",
    "hypher": "^0.2.5",
    "request": "^2.88.0",
    "request-promise-native": "^1.0.7",
    "xregexp": "^4.2.4"
  }
}
